 /** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  // for GitHub Pages with repo name TaxFunded
  // basePath: '/TaxFunded',
  // assetPrefix: '/TaxFunded/',
}
module.exports = nextConfig
